{
  "name": "threejsjourney",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vercel --prod --token $VERCEL_TOKEN",
    "deploy-preview": "vercel --token $VERCEL_TOKEN",
    "deploy-problem": "echo 'chad.is-a.dev' >> dist/CNAME && gh-pages --dist 'dist' --branch 'gh-pages'",
    "deploy2-problem": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "@types/three": "^0.143.0",
    "gh-pages": "^4.0.0",
    "modern-css-reset": "^1.4.0",
    "vercel": "^28.0.1",
    "vite": "^3.0.0",
    "vite-plugin-gh-pages": "^0.3.0"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "gsap": "^3.5.1",
    "lil-gui": "^0.17.0",
    "three": "^0.143.0"
  }
}
